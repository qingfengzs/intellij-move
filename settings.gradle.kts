rootProject.name = "intellij-sui-move"

pluginManagement {
    repositories {
        maven("https://oss.sonatype.org/content/repositories/snapshots/")
        gradlePluginPortal()
    }
}

//include("ui-tests")
